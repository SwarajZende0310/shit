#include "filesystem.h"

bool DirExists(char *dirName, size_t size)
{
    return false;
}

bool createDir(char *dirName, size_t size)
{
    return false;
}

bool createObj(char *objName, char *data)
{
    return false;
}

bool getCurrentPath(char *currPath, size_t *size)
{
    return false;
}
